1. 在新增Account 用户信息后，其绑定的 Task 任务再 点击启动后其状态应该与 `Account Management` 的 `Current Status` 同步，表示这个账户被占用，正在运行中。
2. 是否存在接口，用于影刀应用执行完毕后向网页发起类似webhook的请求，用户在 `Task Control` 点击影刀应用执行按钮后，应该将此任务在 `Execution Logs` 记录，此时状态是运行中  只有影刀应用执行完毕后会发起 请求 告诉网页的`Execution Logs` 此时的结束执行时间(End Time) 再将此账号下最后执行的应用的名称 给到 `Task Control` 页面的 `Last Run` 这样便完成闭环，这一点很重要。
3. 发现前端网页新增的账号信息和任务信息没有在 (../backend/app.db) 下看到数据，可能下一次打开就丢失了，请确保数据库与前端的网页展示信息存在关联，便于持久化存储，若数据结构不正确，可以使用mongodb来保存JSON信息，这样也许可以更清晰，一切以优雅的实现功能为主。